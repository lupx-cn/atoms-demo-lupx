# Atoms-Demo 项目开发目标文档

## 项目定位

对标 [atoms.dev](https://atoms.dev/) 的最小可行AI智能体原型系统。

接收用户自然语言需求，由AI智能体生成完整网页代码，提供**双栏可视化交互**：左侧对话会话面板 + 右侧 iframe 实时网页预览。

本项目为深度智慧笔试测评Demo，遵循 **6~8 小时开发约束**，打造**可运行、可交互、可持久化、可公网部署**的产品原型。

---

## 🎯 核心交付目标（测评硬性要求）

### 1. 基础功能基线

- [x] **前后端分离架构**：FastAPI 提供 SSE（Server-Sent Events）流式接口
- [x] **双栏 UI 布局**：左侧对话会话列表，右侧 iframe 实时预览生成页面
- [x] **自然语言生成网页**：用户输入需求 → AI 流式生成内嵌 CSS/JS 的单文件 HTML
- [x] **多轮对话迭代**：支持基于历史代码进行增量修改，而非每次都重新生成
- [x] **真实人机交互反馈**：请求发送 → 打字机流式输出 → 预览区自动刷新

### 2. 硬性合规项

- [x] **数据持久化**：前端 LocalStorage 保存会话历史，刷新页面数据不丢失
- [x] **云端可部署**：支持 Vercel / Render 等平台部署，产出公网可访问链接
- [x] **源码开源**：代码上传 GitHub，仓库设置为 Public

---

## 💡 创新扩展亮点（拉开评分差距）

> ⚠️ 设计原则：以下亮点均需在 6-8h 内可控实现，**优先保障主流程稳定性**，拒绝过度设计。

- [x] **模拟 Agent 工作流的过程反馈**
  通过前端 UI 状态机（状态卡片序列），模拟展示“需求分析中 → 方案规划中 → 代码生成中 → 渲染预览”的完整链路，让用户感知 AI 的“思考过程”，区别于普通 LLM 的黑盒问答体验。

- [x] **基于上下文的增量迭代能力**
  多轮对话中，自动将**上一轮生成的完整 HTML 代码**作为历史上下文注入后续请求，使 AI 能基于现有代码进行精准的“增量修改”而非“重新生成”，实现真正的对话式迭代开发。

---

## 📌 边界取舍说明（面试重点阐述）

| 舍弃项 | 替代方案 | 取舍理由 |
|:---|:---|:---|
| 用户账号 / 注册登录系统 | 前端 LocalStorage 会话持久化 | 聚焦核心代码生成体验，避免后端鉴权复杂度分散精力 |
| 多文件/大型前端工程生成 | 统一输出单文件 HTML | 大幅降低 AI 生成难度与代码拼接出错率，符合短周期原型定位 |
| 生产级沙箱安全方案 | iframe 基础 sandbox 属性 | Demo 阶段可接受；文档中标注生产环境需补充 CSP 与 DOM 净化策略 |
| 复杂数据库持久化 | LocalStorage | 无需部署数据库服务，0 运维成本，满足“数据持久化”硬性要求 |

---

## 🧪 标准测试用例（验收清单）

按以下顺序执行，所有用例通过方可提交：

1. **初始生成**：输入“生成一个待办事项清单网页” → 验证左侧输出代码、右侧预览正常
2. **迭代修改**：输入“把待办清单的背景色改成深色模式，并增加删除已完成事项的功能” → 验证页面在前一版基础上正确更新
3. **全新生成**：输入“制作一个简易计算器，支持加减乘除运算” → 验证生成全新页面，历史记录保留
4. **二次迭代**：输入“在计算器基础上，将按钮改为圆角并增加点击动画” → 验证增量修改生效
5. **持久化验证**：刷新浏览器 → 验证所有历史对话记录自动加载，预览区恢复最后一次生成结果

---

## 🛠️ 技术选型速览

| 层级 | 选型 | 理由 |
|:---|:---|:---|
| 前端 | 原生 HTML + Vanilla JS + Tailwind CSS（CDN） | 0 构建工具依赖，所有代码可直接在浏览器运行，降低 AI 生成出错率 |
| 后端 | FastAPI + Python | 原生支持 SSE，异步性能好，代码简洁，适合 AI 生成 |
| AI 模型 | DeepSeek API（或兼容 OpenAI 接口的模型） | 成本可控，代码生成能力强，支持流式输出 |
| 持久化 | 前端 LocalStorage | 满足测评硬性要求，无需额外后端存储 |
| 部署 | Vercel / Render | 与 FastAPI 适配良好，支持环境变量配置 API Key，一键部署 |

---

## ✅ 质量门禁（提交前自检）

- [ ] 所有测试用例（1~5）全部通过
- [ ] 部署链接可正常访问，且功能完整
- [ ] GitHub 仓库为 Public，且 README 包含项目简介、启动方式、环境变量说明
- [ ] 代码中无硬编码的 API Key（已使用环境变量）
- [ ] 整体开发耗时控制在 6~8 小时内

---

## 📎 附：开发节奏建议（个人备忘）

| 时间段 | 任务 |
|:---|:---|
| 第 1-2 小时 | 搭建 FastAPI 后端（SSE 接口） + 前端 HTML 骨架（双栏布局） |
| 第 3-4 小时 | 联调流式生成 + iframe 预览更新 + LocalStorage 持久化 |
| 第 5-6 小时 | 多轮迭代逻辑 + 状态机反馈 UI + 测试用例验证 |
| 第 7-8 小时 | 部署上线 + 写 README + 最终自检与 Bug 修复 |