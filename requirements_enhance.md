# 需求细化：代码版本管理 + 会话找回（M4 增强）

> 来源：M4 测试阶段用户反馈。本文档对两个体验问题进行需求细化，供评审后实施。
> 关联文档：goals.md（项目目标）、dev_spec.md（开发规范）、README.md（里程碑）。
> 状态：待评审（评审通过后进入实现，并同步更新 dev_spec / README / work_log）。

---

## 0. 现状问题根因分析

### 问题 A：代码"像在聊天框里生成"，没有版本位置
- 数据层：每条 assistant 消息的 content 保存了该轮完整 HTML（app.js onDone: liveAssistantMsg.content = code），会话对象仅保留最后一份 lastCode，无版本数组。
- UI 层：assistant 消息按普通文本气泡渲染（pre-wrap），无"代码块/文件"形态；无版本标签、无切换回看入口。
- 结论：**数据天然有版本（messages 中每条 done 消息即一版），缺的是"版本结构"与"版本 UI"**。

### 问题 B：新建会话后旧对话"找不见"
- 根因 1：index.html 中 #session-list-wrap 初始 class 含 hidden（默认折叠），首次进入看不到会话列表。
- 根因 2（关键 Bug）："展开/收起"按钮 #btn-collapse-sessions 位于可折叠容器内部；收起后按钮随容器一起消失，**页面上无任何入口可恢复会话列表**。
- 结论：会话数据一直保存在 LocalStorage，未丢失；是 UI 入口失效导致"找不见"。

---

## 1. 需求一：代码版本管理（V1 / V2 / …）

### 1.1 用户故事
- 作为用户，完成一次生成后自动产生 V1；再次迭代生成后自动追加 V2。
- 我可以点击版本标签回看任意一版的预览与代码，而不仅是最新版。
- 我可以基于任意历史版本继续迭代（默认基于最新版）。
- 刷新页面后版本列表与各版本代码不丢失。

### 1.2 功能需求
| 编号 | 需求 | 说明 |
|:---|:---|:---|
| FR1.1 | 版本化存储 | session 增加 versions 数组：{ index, label(V1/V2/…), prompt, code, createdAt }；每次生成 done 时追加新版本，并同步 lastCode 指向最新版 |
| FR1.2 | 版本标签栏 | 右侧预览区顶部（或代码面板标题栏）显示版本标签条，当前版本高亮，可横向滚动 |
| FR1.3 | 版本切换回看 | 点击版本标签：预览 iframe 与该版本代码同步切换；切换时不影响会话消息 |
| FR1.4 | 从指定版本继续迭代 | 选中某版本后发送新需求，history_code 使用该版本代码；新生成结果追加为新版本 |
| FR1.5 | 版本信息展示 | 每个标签展示序号与生成时间（或对应需求摘要），便于区分 |
| FR1.6 | 旧数据兼容迁移 | 加载无 versions 的旧会话时，从 messages 中 status=done 的 assistant 消息按顺序重建版本；不报错、不丢数据 |
| FR1.7 | （可选）版本说明文本 | 生成时 AI 同时输出简短说明/改动点，存入版本 summary，展示在标签或气泡上方 |

### 1.3 数据模型变更（前端）
- 会话对象：{ id, title, messages, lastCode, versions: [], updatedAt }
- versions 元素：{ index: number, label: "V1", prompt: string, code: string, createdAt: number, summary?: string }

### 1.4 验收标准
- AC1-1：同一会话连续 2 次生成后出现 V1、V2 两个标签。
- AC1-2：点击 V1 恢复 V1 预览与代码；点击 V2 恢复 V2，来回切换正常。
- AC1-3：选中 V1 后输入"把背景改成红色"→ 生成 V3，且 V3 确实基于 V1 代码增量修改。
- AC1-4：刷新页面后版本标签、各版本代码与当前选中版本均恢复。
- AC1-5：仅含 lastCode/messages 的旧会话可正常打开，自动重建版本且不报错。

---

## 2. 需求二：会话找回与会话管理

### 2.1 用户故事
- 作为用户，我新建会话后，仍能在可见位置找到"会话"入口，随时展开历史会话列表。
- 我可以快速定位目标会话（搜索/时间排序），并清楚当前处于哪个会话。
- 刷新页面后，会话列表与当前会话保持。

### 2.2 功能需求
| 编号 | 需求 | 说明 |
|:---|:---|:---|
| FR2.1（P0 必修） | 会话入口常驻 | 折叠按钮移出可折叠容器（如左侧栏固定区域），收起列表后仍有可见"会话"入口可展开 |
| FR2.2（P0 必修） | 默认展开 | 首次加载显示会话列表；展开/收起偏好可记忆（LocalStorage） |
| FR2.3 | 当前会话标识 | 列表当前项高亮；消息区顶部/标题栏显示当前会话名 |
| FR2.4 | 会话搜索过滤 | 列表顶部提供关键字输入框，按标题过滤 |
| FR2.5 | 会话管理（可选） | 重命名、删除（带确认）、清空消息 |
| FR2.6 | 排序与信息 | 按 updatedAt 倒序；列表项展示标题 + 时间，便于区分同名会话 |

### 2.3 验收标准
- AC2-1：初始进入页面即显示会话列表（或醒目入口），能看到全部历史会话。
- AC2-2：收起列表后仍存在可见展开入口，点击可恢复列表。
- AC2-3：新建会话后旧会话仍在列表，点击可完整恢复消息与预览。
- AC2-4：刷新后会话列表与当前会话保持；展开/收起状态按偏好恢复。
- AC2-5：输入关键字可过滤出匹配会话；当前会话高亮可见。

---

## 3. 优先级与实施建议

| 优先级 | 内容 | 建议 |
|:---|:---|:---|
| P0 | FR2.1 + FR2.2（会话找回 Bug） | 先行修复，改动小、风险低 |
| P1 | FR1.1~FR1.6（版本管理全量） | 紧随其后，纯前端改动 |
| P2 | FR1.7 说明文本、FR2.4 搜索、FR2.5 重命名/删除 | 视时间与评分取舍 |

- 影响面：仅前端（index.html / app.js / storage.js / state.js / preview.js / style.css，新增 components/versionBar.js）；后端接口无需改动（history_code 已支持任意版本代码）。
- 测试扩展：新增用例 6「会话找回」、用例 7「版本切换与回看」、用例 8「基于历史版本迭代」。
- 数据安全：versions 与 messages 并存，不删除旧字段，保证旧数据可迁移、可回滚。

---

## 4. 边界与取舍
- 版本粒度为"每次生成一个版本"，不做代码行级 diff 对比（P2 可选：版本间 diff 提示）。
- 不做多文件工程生成（延续 goals.md 取舍：统一单文件 HTML）。
- 版本数据存在 LocalStorage，受浏览器配额限制（单版本 HTML 约 20~40KB，数十版本内安全；超限时给出提示，不覆盖旧数据）。

