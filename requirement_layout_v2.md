# 需求追加：三栏布局 + 右侧多功能面板 + 版本隔离 + HTML 导出（v2）

> 来源：用户查看页面后的需求变更/追加（两条消息合并整理），已与用户逐项确认（Q1~Q5）。本文档为**实施依据**。
> 关联文档：goals.md（项目目标）、dev_spec.md（开发规范）、README.md（里程碑）、requirements_enhance.md（版本管理+会话找回）、work_log.md（工作日志）。
> 状态：**已确认（2026-08-11 用户逐项确认 Q1~Q5 见第 9 节；v2.1 确认结论见第 10.5 节），进入实现**。

---

## 0. 现状问题根因分析

### 问题 A：源码文件查看路径不清晰
- 现状：源码只在「右侧底部弹出式代码面板」（`#code-panel`，默认隐藏，靠顶栏「查看代码」按钮开关）；同时 assistant 消息气泡把整段 HTML 源码以纯文本铺在聊天区。
- 结论：源码存在两个展示位置（聊天气泡 + 底部弹出面板），路径不清晰、无编辑器形态、无"文件"概念，与用户期望的「文件资源管理器 + 源代码编辑器」差距明显。

### 问题 B：预览成品页面与需求页面混杂
- 现状：assistant 消息气泡内展示的是整段 HTML 源码文本（含 AI 可能输出的说明文字），视觉上是大片白底/杂乱文字；聊天区与预览区在视觉上都像是"页面"，用户分不清"需求对话页"与"成品网页"。
- 结论：对话说明文字与生成代码混在同一气泡，右侧预览缺少独立的、干净的"浏览器窗口"形态。

### 问题 C：版本切换时说明文字侵入预览
- 现状：V1/V2 切换只更新 iframe 与代码面板，聊天区仍保留全部生成历史（含源码文本与"修复添加按钮"等说明文字），说明文字与预览并存，干扰浏览成品。
- 结论：缺少"对话区 ↔ 预览区"的硬隔离规则。

### 问题 D：无导出能力
- 现状：生成的 HTML 只能留在页面内查看，无法下载到本地独立运行。
- 结论：需要【下载 HTML】能力，满足"原型可带走、可独立打开"的使用场景。

---

## 1. 目标布局（三栏经典布局）

> ⚠️ 协调说明：第一条消息表述为「双栏（左侧对话需求面板 + 右侧一体化工作区）」，追加消息细化为「左会话列表 / 中对话 / 右多功能面板」三栏。本文档按追加消息的**三栏**为准——"左侧对话需求面板"拆为"左栏会话列表 + 中栏对话聊天"，"右侧一体化工作区"即右栏多功能面板。若用户坚持双栏，见 Q1。

| 区域 | 内容 | 与现状的关系 |
|:---|:---|:---|
| 左栏 | 会话列表（项目列表） | 维持现有全部能力（搜索 / 重命名 / 删除 / 折叠 / 当前会话高亮） |
| 中栏 | AI 对话聊天窗口 | 现状左栏的「当前会话标题 + 消息列表 + Agent 状态卡片 + 输入区」整体移入中栏 |
| 右栏 | 统一多功能面板 | 现状右侧预览 + 底部代码面板重构为「Tab1 网页预览 / Tab2 源代码编辑器」 |

- 布局固定：三栏不随内容浮动/重叠；右栏**取消底部弹出代码面板**。
- 宽度：左栏窄宽（约 260~300px）、中栏对话宽（约 360~420px）、右栏自适应剩余；三栏宽度可拖拽调宽为可选项（Q1）。

---

## 2. 需求一：右侧多功能面板（核心改动）

### 2.1 用户故事
- 作为用户，我可以在右侧同一块区域用 Tab 切换「网页预览」与「源代码」，不再需要在底部弹出代码或翻聊天记录找源码。
- 预览时我只看到成品网页本身（模拟浏览器窗口形态），可拖拽/缩放像真浏览器，不被任何对话说明打扰。
- 我可以用 V1/V2 标签回看历史版本，切换后预览区仍是干净的网页。

### 2.2 功能需求
| 编号 | 需求 | 说明 |
|:---|:---|:---|
| FR2.1 | Tab 切换入口 | 右栏顶部右上角「预览 / 代码」Tab（替代顶栏「查看代码/隐藏代码」按钮与底部 code-panel）；默认进入预览 |
| FR2.2 | 网页预览模式 | 模拟浏览器窗口：带窗口 chrome（标题栏 / 地址栏 / 刷新按钮视觉），iframe 渲染当前版本完整 HTML |
| FR2.3 | 预览拖拽 / 缩放 | 预览"浏览器窗口"在右栏画布内可拖动位置、使用右侧手柄缩放（还原真实浏览器体验）；位置与缩放偏好存入 LocalStorage |
| FR2.4 | 版本标签栏 | V1/V2 按钮位于预览模式顶部（右栏内），切换当前会话历史版本；**面板内只渲染网页** |
| FR2.5 | 代码模式 | 同一右栏区域展示完整 HTML 源代码编辑器（替代底部弹出）；支持编辑、保存（更新当前版本并刷新预览）与下载；行号/语法高亮为加分项 |
| FR2.6 | 文件资源管理器 | 代码模式内左侧轻量文件树（当前仅 index.html 单文件，结构为多文件扩展预留），解决"源码文件查看路径不清晰" |
| FR2.7 | 预览区纯净 | 预览面板内不渲染任何 AI 对话说明文字；"修复添加按钮"等文案只出现在中栏聊天记录 |

### 2.3 验收标准
- AC2-1：右栏顶部「预览 / 代码」Tab 可来回切换；代码不再以底部弹出形式存在。
- AC2-2：预览模式只显示成品网页，无任何 AI 说明文字混入。
- AC2-3：预览"浏览器窗口"可拖动、可缩放；刷新后保持位置/缩放（偏好记忆）。
- AC2-4：代码模式展示当前版本完整 HTML；文件树显示 index.html，点击选中高亮。

---

## 3. 需求二：V1/V2 版本切换规则修正

### 3.1 用户故事
- 作为用户，V1/V2 是当前会话内原型的不同迭代版本；切换版本 = 只看那一版的网页。
- 切换后，右侧预览区/代码区只加载对应版本 HTML，清除一切 AI 对话修复说明文本。

### 3.2 功能需求
| 编号 | 需求 | 说明 |
|:---|:---|:---|
| FR3.1 | 版本定义 | V1/V2 = 当前会话内原型的不同迭代版本（沿用现有 `session.versions` 模型，不新增数据结构） |
| FR3.2 | 切换隔离 | 切换版本只更新右栏（预览 iframe + 代码编辑器内容）；预览区只加载对应版本 HTML |
| FR3.3 | 文案归属 | AI 对话解释文案只保留在中栏聊天记录，不侵入右栏预览/代码区 |
| FR3.4 | 聊天区不受影响 | 切换版本不改变中栏聊天记录内容与滚动位置 |

### 3.3 验收标准
- AC3-1：V1 ↔ V2 切换，右栏预览显示对应版本网页，无说明文字。
- AC3-2：切换版本后中栏聊天记录不变。
- AC3-3：【下载 HTML】导出的是当前选中版本的 HTML。

---

## 4. 需求三：导出功能（下载 HTML）

### 4.1 用户故事
- 作为用户，我可以把当前版本原型下载成 `.html` 文件，双击用任意浏览器直接打开、独立运行。

### 4.2 功能需求
| 编号 | 需求 | 说明 |
|:---|:---|:---|
| FR4.1 | 下载按钮 | 预览模式与代码模式内均提供【下载 HTML】按钮（右栏工具栏常驻） |
| FR4.2 | 下载内容 | 下载当前选中版本的完整 HTML（Blob + `<a download>`，文件名 = 会话项目名 + "-V{N}"，如「待办事项清单网页-V1.html」） |
| FR4.3 | 独立运行 | 下载文件为完整单文件 HTML，不依赖 Atoms-Demo 平台；双击即开（若页面引用 CDN 资源需联网，内嵌 CSS/JS 可离线，README 说明该边界） |
| FR4.4 | 空态处理 | 会话无任何版本时按钮置灰并提示「暂无生成内容」 |

### 4.3 验收标准
- AC4-1：预览 / 代码模式点击下载，浏览器下载 `atoms-demo_V{n}.html`。
- AC4-2：双击下载文件，任意浏览器直接打开且功能完整（与页面内预览一致）。

---

## 5. 需求四：对话区与预览区硬隔离（消息渲染调整）

### 5.1 用户故事
- 作为用户，聊天区只放"需求 + AI 对话说明"，代码与成品网页只出现在右侧面板；聊天区不再有大段 HTML 源码文本。

### 5.2 功能需求
| 编号 | 需求 | 说明 |
|:---|:---|:---|
| FR5.1 | 消息卡片化 | assistant 消息在聊天区渲染为简洁结果卡片：版本号（V1/V2）+ 时间 + 需求摘要 + 操作（在预览中查看 / 复制代码），不再整段铺 HTML 源码文本（Q4 确认呈现细节） |
| FR5.2 | 数据兼容 | `messages` 中 assistant.content 继续保留完整 HTML（旧数据不丢）；可选新增 summary 字段存 AI 说明文字（P2，承接 requirements_enhance.md 的 FR1.7） |
| FR5.3 | 隔离规则 | 对话文本只允许出现在中栏聊天框；右栏预览/代码区绝不混入对话说明 |

### 5.3 验收标准
- AC5-1：聊天区无大段 HTML 源码文本；生成结果以卡片呈现。
- AC5-2：旧会话（content=完整 HTML）正常打开，不报错、不丢数据。
- AC5-3：预览区无任何对话说明文字（解决"大片白底 + 杂乱文字"）。

---

## 6. 数据模型与影响面

- 会话对象：维持 `{ id, title, messages, lastCode, versions, activeVersionIndex, updatedAt }`，不破坏旧数据。
- versions 元素：维持 `{ index, label, prompt, code, createdAt }`；可选新增 `summary`（P2）。
- 新增 UI 偏好（LocalStorage）：`rightPanelTab`（预览/代码）、`previewWindowState`（位置/缩放）。
- 涉及文件：`static/index.html`（三栏布局重构）、`static/css/style.css`（Tab / 浏览器窗口 / 文件树 / 结果卡片样式）、`static/js/app.js`（布局联动、Tab 切换、版本隔离、下载）、`static/js/preview.js`（浏览器窗口渲染与拖拽缩放）、`static/js/components/messageItem.js`（消息卡片化）、`static/js/components/versionBar.js`（保留复用）、新增 `static/js/components/fileTree.js`、新增 `static/js/download.js`。
- 后端：**无需改动**（`history_code` 增量机制不变，仍支持任意版本代码注入）。

---

## 7. 优先级与实施建议

| 优先级 | 内容 | 建议 |
|:---|:---|:---|
| P0 | 三栏布局 + 右栏「预览/代码」Tab + 版本切换隔离 + 下载导出 + 消息卡片化 + 代码编辑器（编辑/保存/下载） | 用户核心诉求，一次性交付 |
| P1 | 预览窗口拖拽/缩放 + 文件资源管理器（文件树） | 紧随其后实现 |
| P2 | AI 说明摘要（summary）、多文件工程、行号/语法高亮、代码格式化 | 视时间与评分取舍 |

- 测试扩展：新增用例 9「三栏布局与 Tab 切换」、用例 10「版本切换隔离」、用例 11「HTML 下载与独立打开」。
- 数据安全：不删除 `messages.content` / `versions` 旧字段，保证旧数据可迁移、可回滚。

---

## 8. 边界与取舍

- 延续 goals.md：仍为单文件 HTML 生成；文件资源管理器当前仅 index.html，结构上为多文件扩展预留。
- 下载文件若页面引用 CDN（如 Tailwind CDN）需联网；内嵌 CSS/JS 可离线。README 说明该边界。
- 聊天区不再铺整段代码（避免杂乱），查看源码统一走右栏代码 Tab。

---

## 9. 确认结论（Q1~Q5 已于 2026-08-11 逐项确认）

- **Q1（布局口径）**：✅ 按三栏实施（左会话列表 / 中对话 / 右多功能面板）；三栏宽度支持拖拽调节。
- **Q2（代码模式）**：✅ 代码编辑器需要具备编辑、保存、下载功能（保存 = 更新当前版本代码并刷新预览）。
- **Q3（拖拽缩放）**：✅ 预览"浏览器窗口"在画布内拖拽移动位置，使用右侧手柄缩放。
- **Q4（消息卡片）**：✅ assistant 消息改为版本卡片（版本号 + 时间 + 需求摘要 + 查看源码/复制等操作）。
- **Q5（下载命名）**：✅ 下载文件名 = 会话项目名 + "-V{N}"（如「待办事项清单网页-V1.html」）。



---

## 10. 需求变更 v2.1：三项体验修复（待确认）

> 来源：v2 实现后的用户实测反馈（三条问题）。本文档仅整理需求，**待用户确认后实施**（本轮未改代码）。

### 10.1 问题与根因

| # | 用户反馈 | 现状根因（代码确认） |
|:---|:---|:---|
| P1 | 中栏生成的卡片只能看到最终结果，无法查看生成过程的对话/分析 | 完成卡片仅渲染「版本号 + 需求摘要(60字) + 时间 + 操作按钮」；生成过程阶段（analyzing→planning→generating→rendering）只由临时状态卡片展示，**未持久化**到会话/版本数据 |
| P2a | 右栏源代码框非常窄，只剩一条细线 | 代码模式内 `#file-tree` 固定 `w-44`(176px)，编辑器 `flex-1`；三栏拖拽挤压下右栏剩余宽度不足时编辑器几乎无宽度；右栏无最小宽度保障 |
| P2b | 应「先选 V1/V2，再看预览或代码」 | 版本栏 `#version-bar` 位于**预览面板内部**（`panel-preview` 内），代码模式没有版本选择入口；交互顺序与用户预期相反 |
| P2c | 点击 V1/V2 后出现不明来源的 AI 文字说明 | 两个可能来源：① 版本标签 `title` tooltip（hover 显示「需求：xxx · 时间」，点击后若鼠标停留也会出现）；② 生成 HTML 页面本身内嵌 AI 说明文字（模型偶发输出，渲染进预览 iframe）。需用户确认出现位置 |
| P3 | 预览待办清单页面过于细长，且无法左右拖拽调整大小 | 预览"浏览器窗口"默认宽为画布 86%（三栏挤压下画布本身窄），高 88% → 细长；缩放仅右下角手柄（`browser-resize`，nwse-resize），**无左右边缘水平拖拽手柄** |

### 10.2 需求列表

| 编号 | 需求 | 说明 |
|:---|:---|:---|
| FR-A1 | 生成结果卡片可展开 | 卡片增加「展开详情」入口（▸/▾），点击展开显示该轮完整信息；再次点击折叠 |
| FR-A2 | 展开内容 | 展开区显示：① 用户原始需求全文（prompt）；② 生成时间与版本号；③ 生成过程阶段记录（需求分析→方案规划→代码生成→渲染预览 各阶段时间点）；④ AI 说明文字（如后端输出并有保存） |
| FR-A3 | 过程数据持久化 | `session.versions[]` 元素新增 `process` 字段（阶段→时间戳映射）；生成中 `onStatus` 记录各阶段时间；AI 说明文字存 `summary`/`note` 字段（承接原 FR1.7/FR5.2，P2 可后置） |
| FR-B1 | 版本选择前置 | 版本标签栏（V1/V2）从预览面板内部**上移至右栏顶层**（Tab 切换栏下方或并入工具栏），预览/代码两种模式共用 |
| FR-B2 | 版本联动 | 切换版本后：预览 iframe 与代码编辑器**同步**加载该版本；「下载 HTML」导出当前选中版本（已满足，保持） |
| FR-B3 | 消除版本切换后的 AI 说明文字 | 定位来源后处理：若为版本标签 tooltip → 移除或改为极简（仅「V1 · 时间」）；若为生成 HTML 内嵌说明 → 后端 SYSTEM_PROMPT 加强「输出纯 HTML、禁止说明文字」约束 + 前端渲染前剥离疑似说明段（保守策略，优先约束生成） |
| FR-C1 | 代码模式空间优化 | 文件树默认收窄（如 `w-32`/128px）或支持折叠（点击图标展开/收起）；代码编辑器占主要宽度；右栏设最小宽度（如 480px）防止被挤压成细线 |
| FR-C2 | 预览窗口默认更宽 | 默认宽度提升至画布 90%~94%（且下限约 520px，画布允许时），避免页面细长 |
| FR-C3 | 预览窗口水平拖拽调宽 | 增加**右侧边缘手柄**（ew-resize，水平拖拽调整宽度，同步调整高度比例或仅调宽）；保留右下角手柄；扩大拖拽热区（≥20px）提升易用性 |

### 10.3 验收标准
- AC-1：点击生成结果卡片可展开/折叠；展开可见需求全文、阶段时间、说明文字（如有）。
- AC-2：右栏顶层可切换 V1/V2；切换后预览与代码内容一致；无不明来源的 AI 说明文字。
- AC-3：代码模式编辑器占据主要宽度，不再出现"一条细线"。
- AC-4：预览窗口可左右拖拽调整宽度，默认不再细长。

### 10.4 确认结论（Q1~Q3 已于 2026-08-11 确认）

- **Q1（展开内容）**：✅ 后端不额外输出分析文本；展开卡片显示「需求全文 + 生成过程阶段时间轴 + AI 说明（如有，从前端剥离的说明文字中提取展示）」。
- **Q2（AI 文字位置）**：✅ 已确认来源 = **生成 HTML 页面内嵌的 AI 说明文字**（如 V2 页面内"这是为您修复了…"），渲染在预览 iframe 中；处理方式 = 后端 SYSTEM_PROMPT 加强约束（禁止输出任何说明文字）+ 前端渲染前保守剥离（提取到版本 note 字段，预览/下载/复制均用剥离后代码）。
- **Q3（预览缩放）**：✅ 优先保证生成 HTML **正常完整显示**（解决待办清单被截断问题，默认窗口加宽加高）；预览窗口支持**四边（上/下/左/右）均可拖拽缩放** + 保留右下角对角线手柄。

### 10.5 实施要点（v2.1 已确认）
- 数据模型：`session.versions[]` 元素新增 `process`（阶段→时间戳映射）与 `note`（剥离的 AI 说明文字，可选）字段；生成中 `onStatus` 记录阶段时间。
- 前端：卡片展开区展示需求全文/阶段时间轴/AI 说明；版本栏上移至右栏顶层共用；文件树收窄 + 右栏最小宽度；预览窗口默认约 94%×92% 画布并支持四边拖拽缩放；渲染前剥离生成 HTML 内嵌说明。
- 后端：`SYSTEM_PROMPT` 增加「输出纯 HTML、禁止一切说明文字（含页面内说明段落）」约束。
- **Q1（展开内容）**：生成过程的「分析/描述」目前后端并未输出分析文本（系统提示要求纯 HTML）。展开区是否接受「需求全文 + 阶段时间轴 + AI 说明（如有）」？是否希望后端额外输出一段简短说明（会增加生成耗时/成本）？
- **Q2（AI 文字位置）**：「点击 V1/V2 后出现的 AI 文字说明」具体出现在哪里——预览页面内 / 版本标签悬浮提示 / 其他？便于精准处理来源。
- **Q3（预览缩放）**：预览窗口需要「仅左右（水平）拖拽调宽」，还是「四边/四角均可拖拽缩放」？## 11. 需求变更 v2.2：生成过程内容可见 + 布局微调（已于 2026-08-11 确认）

### 11.1 需求与确认结论 V2.2
- **R1（阶段内容可见）✅ 方案 A（已确认）**：不是让后端"额外"输出两段说明文字，而是**改造现有输出格式**——AI 在输出 HTML 之前，先用轻量标记输出「需求分析」「方案规划」两段文字（`<analysis>…</analysis>`、`<plan>…</plan>`）；后端流式解析，通过新 SSE 事件 `stage_text` 实时下发给前端；前端在**生成中卡片实时流式展示**，并在**完成卡片展开区**按阶段展示。**代码生成 / 渲染预览两阶段不展示文字**（代码即内容、预览即结果），保留阶段时间轴。
- **R2（新会话按钮）✅（已确认）**：`＋ 新会话` 按钮移到最左栏（会话列表栏）**顶部通栏**；顶栏右上角按钮**移除**。
- **R3（代码展示框高度）✅（已确认）**：根因 = `#panel-code` 缺少 Tailwind `flex` 类（`flex-col` 只设置 flex-direction，切到源代码 Tab 时面板退化为 block、内部 textarea 塌缩）。修复 = 补 `flex` 类，代码区占满右栏剩余高度（≈左栏高度略矮），符合预期。

### 11.2 需求列表 
| FR | 需求 | 说明 |
|:---|:---|:---|
| FR-E1 | 阶段内容实时流式展示 | 生成中卡片实时显示当前阶段（需求分析/方案规划）的文字，打字机效果；后端新增 `stage_text` SSE 事件 |
| FR-E2 | 阶段内容展开可见 | 完成卡片展开区按阶段展示需求分析/方案规划全文；新增 `version.stageText` 字段持久化 |
| FR-E3 | 输出格式改造 | 后端 SYSTEM_PROMPT 要求 AI 先输出 `<analysis>`、`<plan>` 标记段再输出 HTML；后端流式解析并剥离标记，`token` 事件只含 HTML（预览/代码不含标记与说明文字） |
| FR-E4 | 新会话按钮移左栏 | 按钮从顶栏移到左栏顶部通栏；顶栏移除按钮 |
| FR-E5 | 代码展示框高度修复 | `#panel-code` 补 `flex` 类，代码编辑区占满右栏剩余高度 |

### 11.3 验收标准
- AC-1：生成时卡片实时滚动显示「需求分析」「方案规划」文字（打字机效果），代码生成阶段恢复显示输出大小。
- AC-2：生成完成后展开卡片可见两阶段文字；预览/代码不含标记与说明文字。
- AC-3：顶栏无「＋ 新会话」按钮；左栏顶部有通栏按钮，点击新建会话。
- AC-4：源代码 Tab 下代码区高度 ≈ 左栏高度（略矮），可显示多行代码。

### 11.4 数据模型与影响面
- `version.process` 保持（阶段→时间戳）；**新增** `version.stageText = { analyzing, planning }`（阶段文字，字符串）。
- `api.js`：新增 `stage_text` 事件分发 → `onStageText(stage, text)`。
- `app.js`：生成流程累积 `stageTexts`，`onDone` 时写入版本；生成中卡片流式区更新。
- `messageItem.js`：生成中卡片新增流式区；完成卡片展开区新增「阶段内容」区块。
- `services/ai_generator.py`：SYSTEM_PROMPT 输出格式改造 + 流式标记解析器 + `stage_text` 事件；标记在解析时剥离，`clean_generated_code` 与前端净化逻辑不受影响。
