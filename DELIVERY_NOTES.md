# Atoms-Demo 笔试交付说明

> 对标 [atoms.dev](https://atoms.dev/) 的 AI Agent 网页生成原型：**输入自然语言需求 → AI 智能体流式生成可运行网页 → 可视化预览 → 多轮增量迭代**。本文档说明实现思路与关键取舍、当前完成程度、后续扩展与优先级。

---

## 0. 一句话定位

Atoms-Demo 复刻了 Atoms 最核心的体验闭环——「用对话驱动 AI 把想法变成可运行的应用」：用户描述需求，智能体规划并生成单文件 HTML，右侧以模拟浏览器窗口实时预览，支持版本回看、基于历史版本继续迭代、导出下载，全程数据不丢。

- **在线 Demo**：待部署后填写（Render 公网链接）
- **源码仓库**：https://github.com/lupx-cn/atoms-demo-lupx （Public）

---

## 1. 实现思路与关键取舍

### 1.1 实现思路

- **最小闭环优先**：先打通「需求 → SSE 流式生成 → 预览 → 持久化」主链路（M1~M3），再叠加增强能力（版本管理、会话找回、三栏布局、导出），最后做 Bug 修复与交付打磨（M4）。
- **智能体化过程反馈**：后端流式下发 `status` 事件驱动前端状态机（需求分析 → 方案规划 → 代码生成 → 渲染预览）；v2.2 起模型先输出 `<analysis>`/`<plan>` 标记段，通过 `stage_text` 事件实时展示 Agent 的"思考过程"，对应 Atoms 多 AI 员工分工协作的透明化体验。
- **增量迭代而非重新生成**：每轮生成自动保存版本（V1/V2…），下一轮把选中版本的完整 HTML 作为 `history_code` 注入模型，实现基于现有页面的精准修改。
- **生成质量双保险**：后端系统提示词硬约束（响应式、布局稳定、纯 HTML 无说明文字）+ 前端/后端双层净化管线（剥离说明文字、清理空标签壳、保留 JS 渲染挂载点）。
- **预览数据持久化（存储桥）**：生成页在 iframe sandbox 内无法访问 localStorage，通过注入内存代理 + `postMessage` 回写，实现「预览中录入的数据按会话持久化、不同会话隔离、刷新不丢」；下载时可选「烘焙数据」或「空模板」。

### 1.2 关键取舍

| 取舍项 | 方案 | 理由 |
|:---|:---|:---|
| 用户账号/注册 | 不做，前端 LocalStorage | 聚焦核心生成体验，避免后端鉴权复杂度 |
| 生成产物形态 | 统一单文件 HTML | 大幅降低 AI 生成出错率，符合短周期原型定位 |
| 持久化 | LocalStorage + 预览存储桥 | 0 运维成本，满足"数据持久化"硬性要求 |
| 沙箱安全 | iframe sandbox + 代码净化 | Demo 阶段够用；生产需补 CSP 与 DOM 净化 |
| Agent 编排 | 单智能体 + 状态机过程反馈 | 6~8h 内可控；多 Agent 编排为后续扩展方向 |
| 技术栈 | FastAPI + Vanilla JS + DeepSeek | 无构建依赖、SSE 天然支持、成本可控 |

---

## 2. 当前完成程度

### 2.1 已完成（可运行）

- **核心链路**：自然语言 → 流式生成（打字机效果）→ iframe 实时预览 → 多轮增量迭代，测试用例 1~4 用真实 DeepSeek API 回归通过。
- **数据持久化**：会话列表/版本/预览数据 LocalStorage 持久化，刷新不丢（测试用例 5 通过）。
- **版本管理**：V1/V2 标签栏、版本回看、基于历史版本继续迭代、旧数据自动迁移。
- **会话找回与管理**：入口常驻、默认展开、搜索/重命名/删除。
- **三栏布局与右栏多功能面板**：左会话/中对话/右预览+代码，宽度可拖拽并记忆；预览模拟浏览器窗口（拖拽/缩放）；代码编辑器可编辑/保存/下载。
- **导出**：下载当前版本 HTML 单文件（文件名=项目名-V{n}），支持「含预览数据 / 空模板」二选一。
- **工程与质量**：README/goals/dev_spec/work_log/两份需求文档齐全；`py_compile`、`node --check`、存储桥 16 项单测与 17 项端到端验证通过。

### 2.2 未完成 / 暂缓

- **公网部署未完成**：代码已推送 GitHub（Public），`render.yaml` 已就绪，但 Render 服务尚未创建，**无在线访问链接**（提交前最大待办）。
- FR1.7「AI 生成说明文本」（P2 暂缓，已部分被 `stageText` 覆盖）。
- 多 Agent 分工编排、多文件工程生成、账号/多用户、后端数据库持久化（超出本次范围，见第 3 节）。

### 2.3 与 Atoms 能力对照

| Atoms 能力（官方） | Atoms-Demo 实现 | 状态 |
|:---|:---|:---|
| 无代码对话式生成网站/应用 | 需求 → 单文件 HTML 生成 | ✅ 核心已实现 |
| AI 员工分工与过程透明 | 状态机过程反馈 + 阶段文字流式展示 | 🟡 简化实现（单智能体） |
| 可视化预览生成的应用 | iframe + 模拟浏览器窗口 | ✅ 已实现 |
| 版本管理与回溯 | V1/V2 标签栏 + 基于历史版本迭代 | ✅ 已实现 |
| Publish 公网链接 | Render 部署 | ❌ 待完成 |
| Share/导出文件 | 下载 HTML（烘焙/空模板） | ✅ 已实现 |
| 数据管理与持久化 | LocalStorage + 预览存储桥 | ✅ 已实现 |
| 集成 GitHub/Supabase/Stripe | — | ❌ 未做 |

---

## 3. 后续扩展与优先级

- **P0（提交前必做）**：Render 部署上线 → 公网回归测试用例 1~5 → 回填在线链接。
- **P1（近期价值最高）**：后端会话持久化（SQLite）为多用户打底；引入多 Agent 分工（产品分析/架构规划/编码/测试，对应 Atoms 的 AI 员工）；支持多文件工程生成（HTML/CSS/JS 分离或前端框架）。
- **P2（差异化亮点）**：版本间 diff 对比、导出 zip 工程包、沙箱安全加固（CSP + DOM 净化）、移动端适配优化、接入 GitHub 仓库联动与 Supabase 数据服务、AI 生成版本说明文本（FR1.7）。

---

## 4. 关联文档

- `README.md`：里程碑 M1~M4 与验收清单
- `goals.md`：项目目标与边界取舍
- `dev_spec.md`：技术规约、SSE 协议、编码约束
- `work_log.md`：逐轮开发日志（时间/文件/改动原因）
- `requirements_enhance.md`：版本管理 + 会话找回需求
- `requirement_layout_v2.md`：三栏布局 + 多功能面板需求